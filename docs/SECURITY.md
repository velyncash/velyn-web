# Security Notes (velyn-web)

- No secrets committed to repo
- HttpOnly cookie sessions expected from backend
- Client never logs sensitive values (PIN, tokens)
- Input validation at UI and API boundary
